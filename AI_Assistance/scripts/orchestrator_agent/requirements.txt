# Orchestrator Agent — Python dependencies
# Install: pip install -r requirements.txt

langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.0
python-dotenv>=1.0.0
httpx>=0.27.0
langfuse>=2.0.0

# --- Optional: Vector Memory (embedded Qdrant, no Docker needed) ---
# Install to enable semantic memory recall and storage
# qdrant-client>=1.9.0
# fastembed>=0.3.0

# --- Optional: Sandboxed Execution (requires E2B_API_KEY in .env) ---
# Install to sandbox AI-generated code and tool calls
# e2b-code-interpreter>=1.0.0

# --- Optional: Signal Preprocessing (lightweight text cleaning) ---
# Heavy install (~hundreds of MB with models); skip if you only want regex cleaning
# py-data-juicer>=1.2.0
